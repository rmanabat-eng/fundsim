# Hosted Postgres connection string (e.g. from Neon). Copy this file to
# `.env` and fill in your real value there — `.env` is gitignored.
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"
